> list all files of the root director
# ls /

> initialize a new git repository
$ git init

> get the current directory
$ pwd

> get the current directory IBM AIX
$ pwd

> Stop HTTP Server on IBM AIX
$ /usr/IBM/HTTPServer/bin/apachectl stop

> Show hidden files in Finder on Apple OSX
$ defaults write com.apple.finder AppleShowAllFiles TRUE

> get the current directory Apple OSX
$ pwd

> print the .env file
$ cat .env

> install lodash locally using yarn
$ yarn add lodash

> reset the ouput
$ clear

> Show the readme
$ cat readme.md

> Install essential build packages
$ sudo apt-get install -y build-essential

> remove Firefox
$ apt-get purge firefox

> deploy MyClass.cls to Salesforce
$ sfdx force:source:deploy -p MyClass.cls

> print 204 + 423
$ echo "204 + 423" | bc

> install Docker from script
$ curl -fsSL https://get.docker.com | sudo sh

> Add a new user
$ sudo useradd username

> {{input}}
